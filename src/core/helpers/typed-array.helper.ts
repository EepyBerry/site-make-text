
export function asUInt32Array(buf: ArrayBuffer) {
  // test for machine endianness (...because otherwise the array will have the wrong component order)
  let isLittleEndian = false


  // reverse if necessary
  if (isLittleEndian) {
    
  }
}
